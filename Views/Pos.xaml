<UserControl x:Class="Yakout.Views.Pos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:Yakout.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Style="{StaticResource user}">
    <Grid>
        <Canvas>
            <Button  Margin="10" Style="{StaticResource btnPages}" >
                <StackPanel>
                    <Image Source="/image/gg.png"/>
                    <TextBlock Text="Point Of Sale" Style="{StaticResource tbPages}"/>
                </StackPanel>
            </Button>

            <Button x:Name="btnBack" Click="btnBack_Click" Style="{StaticResource btnPages}" Canvas.Right="0" Canvas.Bottom="0"
            Command="{Binding RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Window}},Path=DataContext.NavigateMainBackGroundCommand}">
                <StackPanel>
                    <fa:ImageAwesome Icon="Backward" Width="30"/>
                    <TextBlock Text="Back" Style="{StaticResource tbPagesBack}"/>
                </StackPanel>
            </Button>
        </Canvas>
    </Grid>
</UserControl>
