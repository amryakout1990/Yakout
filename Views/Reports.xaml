<UserControl x:Class="Yakout.Views.Reports"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:Yakout.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Style="{StaticResource user}">
    <Grid>
        <Canvas>
            <Button  Margin="10" Style="{StaticResource btnPages}" >
                <StackPanel>
                    <fa:ImageAwesome Icon="File" Width="30"/>
                    <TextBlock Text="Sale Report" Style="{StaticResource tbPages}"/>
                </StackPanel>
            </Button>
            <Button x:Name="btnBack3" Style="{StaticResource ButtonBack}" Canvas.Right="0" Canvas.Bottom="0">
                <StackPanel>
                </StackPanel>
            </Button>
        </Canvas>

    </Grid>
</UserControl>
